import test from 'node:test';
import mapGestures from '../lib/map-gestures.js';

test.todo('map-gestures must have at least one test', t => {
  mapGestures();
  t.assert.fail('Need to write tests.');
});
